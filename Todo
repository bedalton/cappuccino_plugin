Merge CPString and string in js type results
Take Null parameter out of service and apply a '?' to the property name
Fix method call completions to force selectors matching previous. Right now all selectors are suggested, limit it to those that came before